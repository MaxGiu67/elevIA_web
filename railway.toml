[build]
builder = "nixpacks"
buildCommand = "npm ci && npm run build -w @upgrai/web"

[deploy]
startCommand = "npm run start -w @upgrai/web"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
