# Nixpacks configuration for UPGRAI Web

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build -w @upgrai/web"]

[start]
cmd = "npm run start -w @upgrai/web"
